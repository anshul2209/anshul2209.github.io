{
  "name": "anshulbansal",
  "description": "Anshul Bansal Portfolio",
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "dev": "vite dev",
    "build": "vite build",
    "preview": "vite preview",
    "cy:run": "cypress run",
    "cy:open": "cypress open",
    "test": "pnpm run dev & pnpm run cy:run",
    "clean": "rm -rf ./build",
    "deploy": "pnpm run clean && pnpm run build && node ./scripts/deploy.js",
    "deploy:manual": "pnpm run clean && pnpm run build && node ./scripts/deploy.js"
  },
  "dependencies": {
    "axios": "^0.21.1",
    "gh-pages": "^2.2.0"
  },
  "devDependencies": {
    "@sveltejs/adapter-static": "^3.0.10",
    "@sveltejs/kit": "^2.0.0",
    "@sveltejs/vite-plugin-svelte": "^3.0.0",
    "sass": "^1.69.0",
    "svelte": "^4.0.0",
    "svelte-preprocess": "^5.0.0",
    "vite": "^5.0.0"
  }
}
